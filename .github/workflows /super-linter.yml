name: Super Linter

on: [push, pull_request]

jobs:
  lint:
    name: Run Super Linter
    runs-on: ubuntu-latest  

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3  

      - name: Set up R environment
        uses: r-lib/actions/setup-r@v2
        with:
          r-version: '4.x'

      - name: Install R dependencies
        run: |
          Rscript -e 'install.packages(c("dplyr", "zoo", "tidyr", "ggplot2", "moments", "readr", "rugarch", "nortest", "reshape2"))'

      - name: Run Super Linter
        uses: github/super-linter@v5
        env:
          DEFAULT_BRANCH: main  
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}  
          VALIDATE_PYTHON: true  
          VALIDATE_SQL: true  
          VALIDATE_MARKDOWN: true  
          VALIDATE_R: true  
          VALIDATE_R_MARKDOWN: true  
          SQL_LINT_OPTIONS: "--ignore-rules=trailing-whitespace,unrecognized-statement"

      - name: Lint R Scripts
        run: |
          find . -name "*.R" -print0 | while IFS= read -r -d '' script; do
            Rscript -e "lintr::lint('$script')"
          done
